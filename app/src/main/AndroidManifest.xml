<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizzapppro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_mamberroi_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinishScoreActivity"></activity>
        <activity android:name=".ScoreActivity"
            android:label="Puntuaciones"/>
        <activity android:name=".EditProfileActivity"
            android:label="Editar Perfil"/>
        <activity android:name=".CreateProfileActivity"
            android:label="Crear Perfil"/>
        <activity android:name=".ProfilesActivity"
            android:label="Perfiles"/>
        <activity android:name=".Activity5" />
        <activity android:name=".ActivityName" />
        <activity android:name=".Activity4" />
        <activity android:name=".Activity2"
            android:label="Opciones"/>
        <activity android:name=".Activity3"
            android:label="Juego"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>